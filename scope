#include <stdio.h>

int main()
{
int array[10],max;
printf("Enter 10 integer elements\n");
for(int i=0;i<10;i++)
{

    scanf("%d",&array[i]);
}
max=array[0];
for(int i=1;i<10;i++)
{
   if(max<=array[i]) 
   {
       max=array[i];
   }
}
printf("Biggest element in array is %d",max);
    
}
